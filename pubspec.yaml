name: daily_expense_app
description: A Daily Expense tracking app scaffolded with Material 3 and clean architecture.
publish_to: 'none'
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  provider: ^6.0.5
  fl_chart: ^1.1.1
  http: ^0.13.6
  flutter_secure_storage: ^8.0.0
  speech_to_text: ^6.4.0
  permission_handler: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
